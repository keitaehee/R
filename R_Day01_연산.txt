install.packages("data.table")  
library("data.table")

df = fread("")
head(df)	

#####
c() - vector
matrix() - 행렬
data.frame() - 데이터 프레임
list() - 리스트
str() - 객체구조확인

# 삼각함수
sin()
cos()
tan()
pi

# 올림 etc
round() - 반올림
floor() - 내림
ceiling() - 올림
factorial() - 팩토리얼

# 정보확인
str() - 객체구조
summary() - 기술통계
nrow() - 행개수
ncol() - 열개수
dim() - 행/열개수 (Dimension)
colnames() - 변수명 [추후 컬럼 명 변경]

# 